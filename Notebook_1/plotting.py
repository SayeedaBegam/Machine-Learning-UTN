import numpy as np
from matplotlib.axis import Axis
from perceptron import Perceptron


def plot_decision_boundary(axis: Axis, perceptron: Perceptron):
    """Plots the decision boundary of a perceptron with weights 'weights' and
    bias 'bias'.

    Args:
        axis (Axis): the axis (cf. matplotlib docs) to draw on
        perceptron (Perceptron): perceptron object
    """
    ############################################################################
    # YOUR CODE @T
    # TODO: Use the matplotlib axis object (represents the plot) and add a @T
    # decision boundary using the weights and the bias of the given perceptron@T
    ############################################################################
    pass  


    ############################################################################
    # END OF YOUR CODE @T
    ############################################################################
